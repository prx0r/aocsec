# Baseline findings — 2026-09-23

## Critical (fixed)

1. **Dashboard token exposed in chat (2026-09-23).** Token shared in agent
   chat twice. Rotated same day. Old tokens presumed compromised.
2. **GitHub PAT exposed in chat (2026-09-23).** Used for pushes, cleaned
   from all remote configs same day. If not yet revoked server-side, revoke.
3. **Tokens embedded in git remotes (repair, powrobots).** Found during
   audit, removed same day.

## High (open)

4. **No Cloudflare Access on dashboard origin.** Bearer auth added, but the
   tunnel origin is directly reachable. (powops thread 27.4)
5. **Companies House API key on disk + shared in chat.** Present in
   powuk/.env (gitignored, not committed — verified). Rotate if exposed
   beyond this VPS.

## Medium (fixed or accepted)

6. **CSP unsafe-inline (fixed).** Split to static assets, policy tightened.
7. **408 .eml files in powpowpow (audited).** Newsletters/articles only.
   No credentials, no PII beyond sender addresses. Accepted.
8. **URL query tokens (accepted with mitigation).** Kept for SPA compat;
   Bearer preferred for API clients. Tokens don't hit disk logs on loopback.

## Low

9. **MCP payloads >64KB/line.** Client-side truncation risk, documented.

## Scanner notes
- powstock HUMAN_TASKS.md flags are placeholder format strings in setup
  docs (`ghp_xxx...`), not real credentials. Reviewed, clear.
- 2026-09-23: dashboard_token + powuk/.env were 664, set to 600.
